:{$PORT:8080} {
	encode gzip zstd

	@api path /api /api/*
	handle @api {
		reverse_proxy {$API_UPSTREAM:http://127.0.0.1:8000} {
			header_up Host {upstream_hostport}
		}
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
